# Frontend Environment Variables

# API Configuration
# Backend API endpoint (change if your backend runs on a different URL/port)
REACT_APP_API_URL=http://localhost:8000/api

# Optional: Set to true for verbose logging during development
REACT_APP_DEBUG=false
